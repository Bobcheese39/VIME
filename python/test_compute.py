import numpy as np
import pandas as pd


def test_compute():
    # ponytail: placeholder job. Swap in the real computation here.
    return pd.DataFrame({"x": np.arange(10), "y": np.arange(10) ** 2})
