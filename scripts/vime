#!/usr/bin/bash
# Thin wrapper — delegates to the cross-platform Python launcher.
# Kept for backward compatibility on Linux / macOS.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${VIME_PYTHON:-python3}" "${SCRIPT_DIR}/vime_launcher.py" "$@"
